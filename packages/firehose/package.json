{
  "name": "bluesky-firehose",
  "version": "1.0.0",
  "description": "Real-time Bluesky event viewer using Jetstream firehose",
  "main": "dist/index.js",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "start": "npm run build && node dist/index.js",
    "aggregate": "npm run build && node dist/aggregator.js",
    "dev": "tsc && node dist/index.js",
    "clean": "rm -rf dist"
  },
  "keywords": [
    "bluesky",
    "atproto",
    "firehose",
    "jetstream"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "level": "^9.0.0",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/node": "^20.17.6",
    "@types/ws": "^8.5.13",
    "typescript": "^5.7.2"
  }
}
